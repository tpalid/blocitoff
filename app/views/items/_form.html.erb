<%= form_for @item do |f| %>

    <div class='form-group'>
        <%= f.label :item %></br>
        <%= f.text_area :body %>
    </div>
    <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
<% end %>