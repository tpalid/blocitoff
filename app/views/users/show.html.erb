
<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
<h1> <%= @user.name %> </h1>
        <table class="table table-bordered table-striped table-responsive" style="opacity=1">
            <tr class>
                <th class="text-center">Item</th>
                <th class="text-center">Days Left</th>
                <th class="text-center">Complete</th>
            </tr>
            <%= render @items %>
        </table>

<%= render "items/form", locals: { item: @item } %>
</div>
<div class="col-md-2"></div>
</row>
